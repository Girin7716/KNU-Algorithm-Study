# 뉴스 클러스터링

## 문제 풀이

일단 주어진 문자열을 두글자씩 쪼개면서 소문자로 바꾸는데, 이때 특수문자가 들어가있으면 pass 해준다.

그리고 이를 바탕으로 교집합의 개수를 구할 수 있다. 구하는 방법은 rem1과 rem2 중 더 개수가 적은 리스트를 기준으로 해당 리스트를 하나씩 보면서 만약 다른 리스트에 해당 원소가 들어있으면 해당 값을 제거하면서 inter라는 리스트에 넣어준다.

위 과정을 거치면 rem1+rem2는 중복값이 제거되었으므로 합집합이 된다.

그러면 로직에 맞게 계산해주면 된다.