일반적으로 소수 구하는 방법을 사용했을 때 시간초과<br>
에라토스테네스의 체를 사용했으나 시간 초과<br>
시간 초과 원인을 검색 결과 천만 이후에는 소수인 팰린드롬이 없다는 것을 알게됨<br>
for문 돌리는 최대수를 천만으로 잡고 나니 시간 초과 해결할 수 있었음<br><br>

//에라토스테네스의 체<br>
천만만큼의 boolean 형태 배열을 두고 처음에 true로 다 세팅해놓고 for문을 돌리면서<br>
소수의 배수에는 false 표시<br>
만약 값이 true라면 소수라는 의미이므로 값이 true인 경우에만 팰린드롬인지 아닌지 체크<br><br>
StringBuffer를 이용해서 reverse한 string과 원래의 string을 비교 후 같으면 출력
